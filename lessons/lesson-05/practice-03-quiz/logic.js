// logic.js — 判定（計算）だけを担当するファイル
//
// ルール: このファイルに document を書かない。画面のことは何も知らない係。
//        ここが守れていると、画面が無くても判定だけ確かめられる。

// TODO 2: 問題 question と、選ばれた番号 selectedIndex を受け取り、
//         正解なら true、はずれなら false を返す judge を作って export する
//
//         ヒント:
//           - 正解の番号は question.answerIndex に入っている
//           - 「同じかどうか」は === で比べる（比べた結果がそのまま true / false になる）
//           - if を使わずに 1 行で return できる
//         形: export function judge(question, selectedIndex) { return ____ === ____; }
