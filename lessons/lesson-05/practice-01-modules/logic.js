// logic.js — 計算だけを担当するファイル
//
// ルール: このファイルに document は 1 回も書かない。
//        「画面のことを何も知らない」のが、きれいな分け方の目印。
//
// どの関数も、いまの値を引数で受け取って、新しい値を return で返す（第3回の関数の形そのまま）。
// 関数の前に export を付けると「他のファイルに貸し出す」印になる。

// TODO 1-1: いまの値 count を受け取り、1 増やした値を返す increment を作る
//           ヒント: 返すのは count + 1。export を付けるのを忘れない
//           形: export function 名前(引数) { return 計算; }

// TODO 1-2: いまの値 count を受け取り、1 減らした値を返す decrement を作る

// TODO 1-3: 0 を返す reset を作る
//           ヒント: リセットは「いまの値」を使わないので、引数はいらない

// ----- 余裕があれば -----
// incrementBy(count, amount) … count を amount だけ増やした値を返す関数を足してみよう
// （「5 ずつ増やす」ボタン用。increment との違いは「増やす量を引数でもらう」こと）
