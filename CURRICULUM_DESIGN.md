# JavaScript Entry - Engineer Training Design

## 目的

このリポジトリは、JavaScript（以下 JS）を通じて
「エンジニアとしての基礎思考力」を育成することを目的とする。

学習目標は以下の通り。

- 問題を分解できる
- ロジックを構造化できる
- 状態を管理できる
- Git で履歴を管理できる
- AI を "使われる側" ではなく "使う側" として扱える

JS は手段であり、目的ではない。

---

## 対象と前提

- **対象**: 中学 2 年生。Arduino などでプログラミングに触れた経験があり、`if` / `for` / 変数 / 関数の基本概念は理解済み。
- **前提学習**: Code.org での順次・分岐・繰り返しは通過済み、または並行で使える。
- **個人情報**: 本リポジトリは Public のため、生徒実名・学校名・住所などは一切コミットしない。

---

## 全体設計（1 年間 / 月 1 回 / 全 10 回）

1 回あたり **2 時間** の授業 + 自主学習で進める。Phase は以下の 4 段。

### Phase 1: 基礎とブラウザ操作（第 1 回〜第 2 回）

- 変数 / 型 / 条件分岐 / ループ / 配列 / 関数 の復習（中学 1 年以上の内容を中 2 向けに圧縮）
- HTML と JS の接続、DOM（ドム。ブラウザが HTML を操作するための仕組み）取得、イベント処理

目的: 「入力 → 処理 → 出力」を画面上で 1 往復させる。

### Phase 2: ロジック整理と UI 状態（第 3 回〜第 5 回）

- 関数の引数・戻り値、配列の `map` / `filter`
- 状態を持つ UI（カウンター・トグル・フォーム検証）
- モジュール分割と PR（プルリクエスト）運用

目的: 「動く」から「整理されたコード」へ進化させる。
第 5 回終了時点で、生徒がブランチを切って PR を提出できる状態にする。

### Phase 3: Node.js と Web の仕組み（第 6 回〜第 7 回）

- **Node.js 入門**: `node` コマンド、`npm init`、ファイル読み書き、コマンドライン引数
  - 例題として **Arduino から出したセンサーログ（CSV）を集計する CLI** を扱う（Arduino 経験者への動機づけの核）
- Web の仕組み、`fetch`、JSON、公開 API の呼び出し

目的: ブラウザの外で JS が動くことを体感し、データを扱う道具としての JS を理解する。

### Phase 4: フレームワーク・AI・最終制作（第 8 回〜第 10 回）

- **Next.js 体験**（第 8 回）: `create-next-app`、ページ追加、コンポーネント
- **AI 活用**（第 9 回）: 仕様書 → Claude で生成 → 読み解き → 修正
- **最終制作発表**（第 10 回）: Next.js + Cloudflare Workers（Cloudflare が提供する Web 実行基盤）で公開し発表

目的: 自分のテーマで動くアプリを公開し、AI を "加速装置" として使えるようにする。

---

## 10 回の授業ロードマップ（要約）

| 回 | テーマ | 到達点 |
|----|--------|--------|
| 1 | JS 構文基礎 + ローカル Git | 開発環境に触る（実施済み） |
| 2 | ブラウザで動かす（DOM / イベント） | クリックで画面の文字を変えられる |
| 3 | 関数で整理する | `map` / `filter` で配列を扱える |
| 4 | 状態を持つ UI | カウンター・トグル・フォーム検証 |
| 5 | モジュール分割と PR | ブランチ → PR → マージができる |
| 6 | **Node.js 入門（Arduino ログ処理）** | CSV 集計 CLI が書ける |
| 7 | Web の仕組みと外部 API | `fetch` で JSON を取って表示 |
| 8 | Next.js 体験 | ページを 1 枚追加できる |
| 9 | AI で加速する | 仕様 → 生成 → レビュー → 修正 |
| 10 | 最終制作発表 | 公開 URL を提出できる |

詳細な時間配分・自主学習課題・教材リンクは `docs/roadmap.md` を参照。

---

## Git 運用ルール

- リポジトリは Public。**個人情報はコミットしない**。
- 初期は `add / commit / push` を確実に実行できることを重視。
- 第 5 回以降はブランチ → PR → レビュー → マージの流れに移行する。
- Issue を課題の起点にし、コミットメッセージに Issue 番号を含める（例: `#3 add counter`）。
- 回答用ブランチ `answer/lesson-0X-*` を授業ごとに事前作成する。

目的は「実務フローの体験」。

---

## AI の扱い方（Claude を前提）

- 第 1 回〜第 8 回は **生徒は AI を使わない**。手で書いて基礎理解を固める。
- **第 9 回から AI（Claude）を導入**し、以下を学ぶ。
  - 仕様を日本語で書く力
  - AI 出力を批評する力（そのまま貼らない）
  - 改善点を指摘して修正依頼する力
- ハルシネーション（事実と違うのにもっともらしく見える出力）への注意を常に伝える。

AI は代替ではなく、加速装置である。

---

## Arduino 経験者への動機づけ

本講座の生徒は Arduino 経験があるため、以下のポイントで接続する。

- 第 3 回: `map` / `filter` を「センサー値の配列を整える」例で紹介。
- **第 6 回（本命）**: Node.js CLI で「Arduino 由来の CSV ログを集計」する例題。
- 第 7 回: 「Arduino 側は測る、PC 側の JS は貯めて整理する」役割分担の提示。
- 第 10 回: 最終作品テーマ案として「手持ちの Arduino で測ったデータを Web で見られるようにする」を提示（今期は模擬データ可）。

---

## 言語は変わっても応用できるスキル

- **抽象化**: 共通パターンを見つけ、問題を一般化して捉える力
- **分解**: 大きな課題を小さな作業に分け、順序立てて進める力
- **状態管理**: 値の変化を追跡し、画面や処理の整合性を保つ力
- **履歴管理**: Git の履歴で変更理由と経緯を再現可能にする力

---

## この講座の思想

コードを書く人を育てるのではない。

構造を理解し、
問題を定義し、
未来の技術変化に対応できる人間を育てる。

言語は変わる。
構造は変わらない。
